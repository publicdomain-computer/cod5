This is a ??/
single line !!

This is a \
single line too !!

trigraphs ??([ ??)] ??<{ ??>}  ??'^  ??/\ ??!| ??-~ ??=# ???3x?

%:define alternative (good)
#ifndef alternative
#warning alternative doesn''t work...
#endif

dasf
#define t_hk <test_file01.h>
#include t_hk

#if 1

#define mac(o)  printf(o##_go "hello" ??/
		jml_##o##_go "man %d", mix_##o, #o)
//#define mac(o)  printf(o##_go "hello" ??/
//		jml_##o##_go "man %d", mix_##o, #o)
#define _(x) mac(x) 
#define __(x)  #x 

#define return return(remix)
/* go */ /* mixxx */ # /* test */define gotit megaslurf

int main(int argc, char **argv)
{
	return 0;
__( mega (testy, 8, \1) "jml12" );
}
_(gotit);
	mac
(
a3
)
;

ptr = " jml ";
#define f(a, b, c, d) a,b,c,d
#define b(x, ...) f(x, __VA_ARGS__ )
b("jl", "bl", go1, mac(_69)); 
__DATE__
__TIME__
__LINE__
#if 000
__FILE__
#else
__FILE__ GO!!
#endif


#define count_args(...) get_nb_args(__VA_ARGS__, 3, 2, 1)
#define get_nb_args(_1, _2, _3, n, ...) n

#define debug(...) debug_(count_args(__VA_ARGS__), __VA_ARGS__)

#define debug_(a, ...) debug##a(__VA_ARGS__)

#define debug1(a) debug_##a();
#define debug2(a, b) debug1(a) debug_##b();
#define debug3(a, b, c) debug2(a, b) debug_##c();

debug(jml)
debug(one, two, three)

WIN32
UNIX

#include <test_file03.c>
#endif
UNIX

//#include "test_file02.h"

__nothrow
__post

#define hash_hash # ## #
#define mkstr(a) # a
#define in_between(a) mkstr(a)
#define join(c, d) in_between(c hash_hash d)
char p[] = join(x, y); // equivalent to
// char p[] = "x ## y";

